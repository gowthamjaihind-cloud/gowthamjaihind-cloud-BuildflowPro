# Rendered output: MP4s, frame JPEGs, generated HTML, preview PNGs.
# The inputs (screens/, scripts, narration) are tracked; the renders are not.
out/

# The voice model. ~58 MB of weights, fetched by voice/fetch-voice.sh.
voice/model/

# Rendered audio and the generated manifests that go with it.
films/out/

# Python bytecode from the voice scripts.
__pycache__/
