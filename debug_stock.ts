import { initializeApp } from "firebase/app";
import { getFirestore, doc, collection, getDocs } from "firebase/firestore";
// Need service account... wait, it's easier to just use a custom component!
